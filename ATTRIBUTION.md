# Attribution

Lerpa UI is MIT-licensed (see `LICENSE.md`). Several animated/creative components are
**adapted from or inspired by** open-source libraries. Those projects are MIT-licensed,
which permits reuse **with attribution** — this file provides it. Retain the upstream
copyright notices when redistributing these components.

> Provenance audit: **complete.** Upstream MIT attribution headers have been added to the
> adapted source files (both the `packages/ui` library sources and their `apps/docs` demo
> copies). Each adapted component now carries an in-file credit to its upstream project.

## React Bits — MIT © David Haz
https://github.com/DavidHDev/react-bits

Components in `packages/ui/src/components` adapted from / inspired by React Bits:

- `Hyperspeed`
- `Ballpit`
- `SplashCursor`
- `TrueFocus`
- `Magnet`
- `TiltedCard`
- `SplitText`
- `BlurText`
- `Orbit`
- `Sparkles`
- `DecryptedText`
- `GlitchText`
- `ShinyText`

## Aceternity UI — MIT © Manu Arora
https://ui.aceternity.com

Spotlight / glow / 3D-card style effects in the creative set draw on patterns popularized by
Aceternity UI (free tier, MIT). Audit the creative/animation components for any direct
adaptations and credit accordingly.

## Original Lerpa UI components

The signature set is original to Lerpa UI (no upstream): `AuroraStatCard`, `MagneticButton`,
`SegmentedBillingToggle`, `SpotlightFeatureCard`, `TiltBentoGrid`, `LiveStatusBeacon`,
`GradientBorderCard`, `StepProgressTracker`, `RatingStars`, `AvatarStack`. The app/blocks
components (account, dashboard, ecommerce, auth, etc.) are original implementations.
